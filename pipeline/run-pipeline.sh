#!/usr/bin/env bash
# Coverage Desk orchestrator — sequences Steps 1 through 5b.
#
# STATUS: placeholder, not yet built. The real orchestration logic is
# Phase 2 of the project plan (COVERAGE-DESK-SPEC.md, section 8) — built
# after Phase 1 (a hand-run prototype on one real topic) has proven the
# prompt wording and the feedback/decision JSON shape.
#
# Usage (once built): ./run-pipeline.sh "<topic>" "<notes>"

set -euo pipefail
echo "run-pipeline.sh is not built yet — see COVERAGE-DESK-SPEC.md, Phase 2." >&2
exit 1
